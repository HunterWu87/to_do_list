# .gitignore 文件
# 用于指定Git应该忽略的文件和目录
# 这些文件通常是本地生成的，不需要提交到版本控制系统

# ----------------------------------------
# Python虚拟环境
# 虚拟环境包含项目特定的Python解释器和依赖包
# 这些文件应该由每个开发者在本地生成，不应提交到仓库
# ----------------------------------------
.venv/
venv/
ENV/
env/

# ----------------------------------------
# Python缓存文件
# Python解释器生成的缓存文件，用于加速代码执行
# 这些文件是自动生成的，不需要版本控制
# ----------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# ----------------------------------------
# 数据库文件
# 本地数据库文件和数据目录
# 包含用户数据，应该在本地生成，不应提交到仓库
# ----------------------------------------
*.db
*.sqlite
*.sqlite3
data/

# ----------------------------------------
# 日志文件
# 应用程序生成的日志文件
# 这些文件通常包含运行时信息，不需要版本控制
# ----------------------------------------
*.log
logs/

# ----------------------------------------
# IDE相关文件
# 各种集成开发环境(IDE)生成的配置文件和目录
# 这些是特定于开发者环境的，不应提交到仓库
# ----------------------------------------
.idea/
.vscode/
*.swp
*.swo
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ----------------------------------------
# 临时文件
# 测试和构建过程中生成的临时文件
# 这些文件可以在本地重新生成，不需要版本控制
# ----------------------------------------
.coverage
htmlcov/
.pytest_cache/
.tox/
.nox/
.hypothesis/
.coverage.*
coverage.xml
*.cover
.hypothesis/

# ----------------------------------------
# 本地配置文件
# 包含特定于本地环境的配置
# 这些文件通常包含敏感信息或特定于环境的设置
# ----------------------------------------
local_settings.py
instance/
.webassets-cache

# ----------------------------------------
# 其他常见忽略文件
# 操作系统生成的文件和其他不需要版本控制的文件
# ----------------------------------------
.DS_Store
Thumbs.db
desktop.ini 